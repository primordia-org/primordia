# Bump Bun to 1.3.14

Updated the Bun runtime version pinned in `mise.toml` from `1.3.13` to `1.3.14`.

This keeps Primordia worktrees and deployment environments on the latest requested Bun patch release while preserving the existing mise-based runtime versioning flow.
