# Update landing page messaging

Refreshed the landing page copy to describe Primordia as "the anything app with agentic coding built in" instead of leaning on self-writing/self-modifying language.

The surrounding hero, feature, section, footer, and metadata copy now frames the value more practically: an AI coding agent built directly into the app, with previews and deployment handled in-product rather than through a separate external coding tool.
