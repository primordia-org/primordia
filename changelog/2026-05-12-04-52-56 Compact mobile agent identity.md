# Compact mobile agent identity

On small screens, agent identity lines now show the billing source and harness as icons only while keeping the model name visible. This preserves the same status context in cramped layouts without forcing long labels to truncate or crowd the row.
