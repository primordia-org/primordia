# Clarify cloned repo setup

Updated the setup instructions in `README.md` and `AGENTS.md` to tell users and agents who have already cloned Primordia to run `./scripts/install.sh` from the repository root instead of using the remote curl installer.

This keeps the one-line install path for fresh installs while making the cloned-repository path simpler and more direct.
