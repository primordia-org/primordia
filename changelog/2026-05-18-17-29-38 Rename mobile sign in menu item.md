# Rename mobile sign in menu item

Changed the authenticated hamburger menu QR sign-in action label from "Sign in on another device" to "Mobile sign in".

This keeps the menu item shorter so it takes up less space while preserving the same cross-device sign-in behavior.
