# Keep changed files visible while running

The evolve session changed-files panel now remains visible while the agent is running instead of disappearing until the thread becomes ready again.

While work is still in progress, the panel is visually marked as potentially stale and includes a Reload button so users can manually refresh the diff summary and see newly written files sooner.
