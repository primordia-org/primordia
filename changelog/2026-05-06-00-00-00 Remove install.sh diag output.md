# Remove install.sh diagnostic output

Removed the `[diag]` log lines from `scripts/install.sh` that printed internal
install state (`HOSTNAME_FQDN`, `PROBABLY_A_SERVER`, `REVERSE_PROXY_PORT`,
`PROXY_RUNNING`, `PROXY_CHANGED`, `SERVICE_CHANGED`). These were added for
debugging during initial setup and are no longer needed.
