# Make ANSI logs horizontally scrollable

Updated the shared ANSI log renderer so long terminal output can be scrolled sideways instead of being clipped on narrow screens. This keeps deploy and server logs readable on mobile when commands or diagnostics extend beyond the viewport width.
