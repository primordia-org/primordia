# Add weighted progress ticks

Progress bars can now render visual tick marks at supplied progress positions. The evolve session to-do progress bar uses each task's weight to place those ticks at task boundaries, making it clear how large each step is relative to the overall weighted task list.
