# Fix branches running agent label

Changed the `/branches` status label for sessions in the `running-claude` state from “running claude…” to “running agent…”. This keeps the branches page wording accurate now that evolve sessions can run through multiple agent harnesses, not only Claude Code.
