# Fix branch tree wrapping

Updated the `/branches` page so each branch row stays on one line and branch tree sections scroll horizontally when branch names or action controls are too wide for the viewport.

This preserves the visual tree connectors instead of allowing wrapped row content to appear under the wrong branch level.
