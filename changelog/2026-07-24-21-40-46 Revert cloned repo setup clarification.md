# Revert cloned repo setup clarification

Reverted the README and agent setup guidance added by the "Clarify cloned repo setup" change, including the extra instruction to run `./scripts/install.sh` from an already-cloned repository.

That clarification made the install path harder for Shelley, so the docs now return to the simpler one-line installer-first guidance while keeping manual development notes separate.
