# Update landing page messaging

## What changed
- Changed the hero sub-headline from "The web application that rewrites itself on demand." to "The web application that writes itself."
- Changed the install script footnote from "Runs on your laptop · requires an exe.dev account with SSH configured" to "Script requires an exe.dev account and SSH"
- Removed the "Self-modifying · Powered by Claude" badge from the hero section
- Removed the animated gradient blob background (blue/indigo, violet/purple, fuchsia/pink blobs + grid overlay) from the hero section

## Why
Tightened the landing page copy and visuals to be more concise and clean.
