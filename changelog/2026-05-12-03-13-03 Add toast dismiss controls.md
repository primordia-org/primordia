# Add toast dismiss controls

Added a close button to the floating "Request submitted" / "View session" toast shown after submitting an evolve request from the floating dialog.

The toast can now also be swiped horizontally to dismiss it on touch or pointer devices, giving users a quick way to clear the notification without waiting for the automatic timeout.
