# No-op change

Adds `.next/dev/dev/types/**/*.ts` to the `include` array in `tsconfig.json` so TypeScript picks up the nested Next.js dev-mode generated type declarations.

No application logic or behaviour is changed.
