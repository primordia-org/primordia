# No-op change

Reverts a mistaken addition of `.next/dev/dev/types/**/*.ts` to the `include` array in `tsconfig.json`.
That path has a doubled `dev` segment and does not exist; the correct path `.next/dev/types/**/*.ts` was already present.

No application logic or behaviour is changed.
