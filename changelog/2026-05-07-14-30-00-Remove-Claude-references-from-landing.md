# Remove Claude references from landing page

## What changed
- Updated `app/LandingSections.tsx`: replaced "Claude Code" with "An AI agent" in feature description and how-it-works step
- Updated `README.md`: replaced "Claude" references with general terminology throughout

## Why
The landing page should use general terminology rather than naming a specific AI provider. This makes the messaging more inclusive and agnostic to the underlying AI implementation.
