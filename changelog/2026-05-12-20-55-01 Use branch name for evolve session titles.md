# Use branch name for evolve session titles

Changed the Evolve session page metadata so the browser document title uses the session branch name instead of the generic "Evolve Session" label.

This makes it easier to distinguish multiple open evolve sessions by their browser tabs.
