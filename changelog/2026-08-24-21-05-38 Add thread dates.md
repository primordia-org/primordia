# Add thread dates

Thread detail views now show calendar dates directly in the request cards: the original “Your request” card shows the created date and current last-worked date, while each “Follow-up request” card shows the day that follow-up work was submitted. This keeps date context attached to the user request that caused the work, without showing exact times.
