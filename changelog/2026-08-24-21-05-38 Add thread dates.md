# Add thread dates

Thread views now show calendar dates for when a thread was created and when it was last worked on. The thread detail page displays these dates in the thread summary card, and the threads graph/diagnostics include the same date metadata so users can quickly understand thread recency without needing exact timestamps.
