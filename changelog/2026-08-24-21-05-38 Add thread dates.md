# Add thread dates

Thread detail views now show each request's calendar date in the top-right of its request card. The original “Your request” card and each “Follow-up request” card use the timestamp already recorded on that request event, keeping date context attached to the user request that caused the work without adding new structured log or database fields.
